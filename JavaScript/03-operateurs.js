//Opérateurs de comparaisons

("==, != , ===, !==, >, < , >=, <=");

5 == 5;
5 != 4;
5 === "5";
5 !== "4";

//Opérateurs logiques

("&&"); //et
("||"); //ou
("!"); //non

//Opérateurs d'affectation

("+=, -=, *=, /=, %=");

x += 5; // x = x + 5

//Condition if

if (x) {
  y;
} else {
  z;
}

//Condition switch
switch (key) {
  case value:
    //action
    break;

  default:
    //action
    break;
}
